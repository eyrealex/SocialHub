<% comments.each do |comment| %>

  <div class="comment">
    <%= link_to comment.account.full_name, profile_path(comment.account.username) %>
    <%= simple_format comment.message %>
    <small><%= link_to "Edit" %> | <%= link_to "Delete" %></small>


  </div>
<% end %>
